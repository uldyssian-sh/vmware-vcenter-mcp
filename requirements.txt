# Core dependencies for development environment
# Note: In production, install only required packages based on your needs

# Basic Python utilities
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.28.0

# Optional MCP dependencies (install if using MCP framework)
# mcp>=1.0.0
# pydantic>=2.0.0

# Optional VMware dependencies (install if connecting to real vCenter)
# pyvmomi>=8.0.0
# vsphere-automation-sdk-python>=1.80.0

# Optional web framework (install if using REST API)
# fastapi>=0.100.0
# uvicorn>=0.20.0

# Optional security (install if using encryption)
# cryptography>=40.0.0

# Development and testing
pytest>=8.0.0
pytest-asyncio>=0.21.0